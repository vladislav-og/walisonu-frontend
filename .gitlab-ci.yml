stages:
  - build

build walisonu:
  stage: build
  image: node:11-alpine
  tags:
    - words
  script:
    - cd frontend
    - npm install
    - npm run build
    - rm -rf /home/gitlab-runner/public
    - mkdir /home/gitlab-runner/public
    - mv frontend/build /home/gitlab-runner/public
